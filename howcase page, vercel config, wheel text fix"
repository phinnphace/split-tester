[1mdiff --git a/phase10_split_comparison.py b/phase10_split_comparison.py[m
[1mindex adb8ba1..519fd22 100644[m
[1m--- a/phase10_split_comparison.py[m
[1m+++ b/phase10_split_comparison.py[m
[36m@@ -117,14 +117,18 @@[m [mprint()[m
 images, labels = load_all_data()[m
 print(f"Total images: {len(images)} ({sum(labels)} da, {len(labels)-sum(labels)} other)")[m
 [m
[31m-# Fixed shuffle order for all splits[m
[32m+[m[32m# Fixed shuffle order for all splits.[m
[32m+[m[32m# NOTE: SystemRandom is NOT seeded — the shuffle is fixed *within* a run (all five[m
[32m+[m[32m# splits share it, so the ratio is the only variable), but a re-run draws a new[m
[32m+[m[32m# shuffle and will produce different numbers. FIXED_SEED covers model init/training.[m
 rng = secrets.SystemRandom()[m
 indices = list(range(len(images)))[m
 rng.shuffle(indices)[m
 [m
 results = {}[m
 for split in SPLITS:[m
[31m-    split_name = f"train_{int(split*100)}_val_{int((1-split)*100)}"[m
[32m+[m[32m    # round(), not int(): int((1-0.8)*100) truncates to 19 (float error) -> "val_19"[m
[32m+[m[32m    split_name = f"train_{round(split*100)}_val_{round((1-split)*100)}"[m
     split_idx = int(len(indices) * split)[m
     train_idx = indices[:split_idx][m
     val_idx = indices[split_idx:][m
[36m@@ -146,6 +150,7 @@[m [mprint(f"\nSplit ratio window:")[m
 for name, r in results.items():[m
     print(f"  {name}: {r['val_accuracy']:.4f}")[m
 [m
[32m+[m[32mos.makedirs('models', exist_ok=True)[m
 with open('models/split_window.json', 'w') as f:[m
     json.dump(results, f, indent=2)[m
 print("\nSaved to models/split_window.json")[m
[1mdiff --git a/split_window.json b/split_window.json[m
[1mindex e54acbf..f236b5c 100644[m
[1m--- a/split_window.json[m
[1m+++ b/split_window.json[m
[36m@@ -14,12 +14,12 @@[m
     "val_size": 147,[m
     "val_accuracy": 0.673469387755102[m
   },[m
[31m-  "train_80_val_19": {[m
[32m+[m[32m  "train_80_val_20": {[m
     "train_size": 391,[m
     "val_size": 98,[m
     "val_accuracy": 0.7346938775510204[m
   },[m
[31m-  "train_90_val_9": {[m
[32m+[m[32m  "train_90_val_10": {[m
     "train_size": 440,[m
     "val_size": 49,[m
     "val_accuracy": 0.8163265306122449[m
